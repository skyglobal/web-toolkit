deployment:
  production:
    branch: master
    commands:
      - bundle exec rake deploy
  feature:
    branch: /feature-.*/
    commands:
      - bundle exec rake deploy_feature
machine:
  ruby:
    version: ruby-1.9.3-p392