version: 1.9.2
destination: _site
source: .
exclude: [
  grunt,
  node_modules, test, README.md,
  Gemfile, Gemfile.lock, rakefile,
  Web-toolkit.iml, circle.yml, Gruntfile.js,package.json,
  build.txt]
include: [_includes, lib]
port: 4000
